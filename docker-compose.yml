services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: personal_worklog
    ports:
      - "5432:5432"
    volumes:
      - worklog_pg:/var/lib/postgresql/data

volumes:
  worklog_pg:
